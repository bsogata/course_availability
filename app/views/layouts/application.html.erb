<!DOCTYPE html>

<!--
  The basic framework for all pages in this application.
	
	This loads in all of the stylesheets and JavaScript code used across the site.
	The body then renders the header and content for the current page.
  
  Author: Hansen Cheng
          Branden Ogata
-->  

<html>
<head>
	<title><%=yield(:title)%></title>
	<%= stylesheet_link_tag    "application", media: "all" %>
	<%= javascript_include_tag "application" %>
  <script src="http://ajax.aspnetcdn.com/ajax/jQuery/jquery-1.10.2.min.js"></script>	
	<%= csrf_meta_tags %>
	<%= render 'layouts/shim' %>
	<!--[if lt IE 9]>
	<script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
	<![endif]-->
</head>
<body>
	<%= render 'layouts/header' %>
	<div class="container">
		<% flash.each do |key, value| %>
			<div class="alert alert-<%= key %>"><%= value %></div>
		<% end %>	
		<%= yield %>
	</div>
</body>
</html>
